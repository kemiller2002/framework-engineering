# Evaluations

Create blinded score sheets here after raw outputs have been normalized. Name each record with the output artifact ID, not the framework identity. Preserve independent evaluator records before reconciliation.
