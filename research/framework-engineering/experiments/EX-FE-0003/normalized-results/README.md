# Normalized Results

Store evaluator-visible copies of raw outputs with only transport metadata removed. Each record must preserve the raw artifact hash and an opaque output ID. Do not repair, summarize, or relabel content in normalization.
